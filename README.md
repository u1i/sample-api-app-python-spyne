# Sample Python API app using Spyne Framework

### Usage
* install Python modules from requirements.txt using pip
* run ./start_listener.sh to run the API app
* run ./request.sh in another terminal window to trigger a request